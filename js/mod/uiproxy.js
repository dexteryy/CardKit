define('uiproxy', [
    'dollar',
    'mo/lang'
], function($, _) {
    var exports = {
        bind: function(node, events) {},
        unbind: function(node, events) {}
    };

    return exports;
});
